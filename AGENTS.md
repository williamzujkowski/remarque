# Remarque — Agent Rules

**Canonical file: [`AGENT_RULES.md`](AGENT_RULES.md).**

This file is intentionally a pointer, not a copy — it was previously a byte-identical duplicate of `AGENT_RULES.md` with no sync mechanism, which guaranteed eventual drift (issue #46).

Agents working in this repo: load **`AGENT_RULES.md`** (implementation contract) and **`REMARQUE.md`** (system specification) before making changes. Token values live in **`tokens.css`**; fonts in **`fonts.css`**.
